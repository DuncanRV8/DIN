import 'package:flutter/cupertino.dart';

class PagesImageScreen extends StatelessWidget {
  const PagesImageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
